<%
  /* Restricted states are dignified and explanatory, and reveal no protected
     metadata — no counts, no names, no hint that anything specific exists
     (Shared Foundation v1.4 section 5). */
%>
<div class="state state--restricted">
  <p class="state__title"><%= states.restricted %></p>
  <p><%= card.statusReason %></p>
</div>
