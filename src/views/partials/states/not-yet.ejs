<%
  /* An offering LiF has intentionally chosen to announce before it opens.
     Clearly labelled, never actionable, and it leaves no dead control behind
     (Dashboard Unified v1.2 section 19.6). */
%>
<div class="state state--not-yet">
  <p class="state__title"><%= states.notYetAvailable %></p>
  <p><%= card.statusReason %></p>
</div>
